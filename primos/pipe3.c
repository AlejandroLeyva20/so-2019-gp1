#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(){
    int n;
    scanf("%d\n",&n);
    if(n == -1){
        fprintf(stderr, "^^ Está es la lista de números primos del 1 al 100\n");
    }
}